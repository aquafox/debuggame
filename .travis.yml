language: cpp
compiler:
  - gcc
# Change this to your needs
before_install:
	- sudo apt-get update -qq
	- sudo apt-get install -qq libtinyxml2-2 libtinyxml2-dev libsdl2-2.0-0 libsdl2-dev libsdl2-image-2.0-0 libsdl2-image-dev libsdl2-ttf-2.0-0 libsdl2-ttf-dev
script: cmake . && make